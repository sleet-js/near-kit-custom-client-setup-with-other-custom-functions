// ================================================
export interface ref_args_params_interface {
  pool_id: number;
  from_index: number;
  limit: number;
  fee: number;
  tokens: string[];
  account_id: string;
  token_id: string;
  amount: string;
  deposit: string;
  token_in: string;
  amount_in: string;
  token_out: string;
  min_amount_out: string;
  referral_id: string;
}
// ================================================
